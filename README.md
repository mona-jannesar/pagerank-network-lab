# pagerank-network-lab-
A small Python project that implements the PageRank algorithm from scratch, validates it with PyTest (including comparison to NetworkX), and runs tests automatically via GitHub Actions CI.
